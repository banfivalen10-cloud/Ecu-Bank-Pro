import { useState } from "react";
import { Link, useLocation } from "wouter";
import { Button } from "@/components/ui/button";
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger } from "@/components/ui/dropdown-menu";
import { toast } from "sonner";
import { FolderSearch, Layers, Menu, Monitor, Smartphone, Cpu, User, LogOut } from "lucide-react";

const SESSION_KEY = "ecu-bank-pro-session";

export function Navbar() {
  const [, setLocation] = useLocation();
  const [deviceMode, setDeviceMode] = useState<"pc" | "mobile">("pc");
  const [lang, setLang] = useState<"ES" | "EN">("ES");
  const session = typeof window !== "undefined" ? localStorage.getItem(SESSION_KEY) : null;
  const logout = () => { localStorage.removeItem(SESSION_KEY); toast.success("Sesión cerrada"); setLocation("/"); };
  const selectDeviceMode = (mode: "pc" | "mobile") => { setDeviceMode(mode); toast.info("La vista se adapta automáticamente al dispositivo."); };

  return <header className="sticky top-0 z-50 border-b border-white/10 bg-[#050811]/85 backdrop-blur-md"><div className="max-w-7xl mx-auto px-3 sm:px-6 lg:px-8 min-h-16 py-2 flex items-center justify-between gap-2"><div className="flex items-center gap-3 sm:gap-6 min-w-0"><Link href="/" className="flex items-center gap-2 sm:gap-3 group shrink-0"><div className="h-9 w-9 rounded-xl bg-gradient-to-tr from-cyan-500 to-blue-600 flex items-center justify-center text-slate-950 shadow-lg shadow-cyan-500/20"><Cpu className="h-5 w-5" /></div><div><span className="font-extrabold tracking-tight text-sm sm:text-xl text-white whitespace-nowrap">Ecu<span className="text-cyan-400"> Bank Pro</span></span><span className="hidden sm:inline-block ml-2 text-[10px] uppercase tracking-wider text-slate-400 font-semibold px-2 py-0.5 rounded bg-white/5 border border-white/10">ECU PLATFORM</span></div></Link><nav className="hidden md:flex items-center gap-1 text-sm"><Link href="/" className="px-3 py-1.5 rounded-lg text-slate-300 hover:text-white hover:bg-white/5">Módulos</Link><Link href="/drive-explorer" className="px-3 py-1.5 rounded-lg text-slate-300 hover:text-white hover:bg-white/5 flex items-center gap-1.5"><FolderSearch className="h-4 w-4 text-cyan-400" />Buscador Drive</Link></nav><DropdownMenu><DropdownMenuTrigger asChild><Button variant="ghost" size="icon" className="flex md:hidden h-9 w-9 border border-white/10 text-slate-300"><Menu className="h-5 w-5" /></Button></DropdownMenuTrigger><DropdownMenuContent align="start" className="w-52 bg-slate-950 border-white/15 text-slate-200"><DropdownMenuItem onClick={() => setLocation("/")}><Layers className="h-4 w-4 mr-2 text-cyan-400" />Módulos</DropdownMenuItem><DropdownMenuItem onClick={() => setLocation("/drive-explorer")}><FolderSearch className="h-4 w-4 mr-2 text-cyan-400" />Buscador Drive</DropdownMenuItem></DropdownMenuContent></DropdownMenu></div><div className="flex items-center gap-1.5 sm:gap-3 shrink-0"><div className="flex items-center text-xs bg-slate-900 border border-white/10 rounded-lg p-0.5"><button aria-label="Vista PC" onClick={() => selectDeviceMode("pc")} className={`h-7 w-7 sm:w-auto sm:px-2 rounded flex items-center justify-center gap-1 ${deviceMode === "pc" ? "bg-cyan-500 text-slate-950" : "text-slate-400"}`}><Monitor className="h-3.5 w-3.5" /><span className="hidden sm:inline">PC</span></button><button aria-label="Vista móvil" onClick={() => selectDeviceMode("mobile")} className={`h-7 w-7 sm:w-auto sm:px-2 rounded flex items-center justify-center gap-1 ${deviceMode === "mobile" ? "bg-cyan-500 text-slate-950" : "text-slate-400"}`}><Smartphone className="h-3.5 w-3.5" /><span className="hidden sm:inline">Móvil</span></button></div><div className="hidden sm:flex items-center text-xs bg-slate-900 border border-white/10 rounded-lg p-0.5"><button onClick={() => setLang("ES")} className={`px-2 py-1 rounded ${lang === "ES" ? "bg-cyan-500 text-slate-950" : "text-slate-400"}`}>ES</button><button onClick={() => setLang("EN")} className={`px-2 py-1 rounded ${lang === "EN" ? "bg-cyan-500 text-slate-950" : "text-slate-400"}`}>EN</button></div>{session ? <DropdownMenu><DropdownMenuTrigger asChild><Button variant="outline" size="sm" className="border-white/15 bg-white/5 text-white"><User className="h-4 w-4 mr-1 text-cyan-400" /><span className="hidden sm:inline max-w-[100px] truncate">{session}</span></Button></DropdownMenuTrigger><DropdownMenuContent align="end" className="bg-slate-950 border-white/15 text-slate-200"><DropdownMenuItem onClick={logout} className="text-red-300"><LogOut className="h-4 w-4 mr-2" />Cerrar sesión</DropdownMenuItem></DropdownMenuContent></DropdownMenu> : <Button variant="outline" size="sm" onClick={() => setLocation("/login")} className="border-white/20 bg-transparent text-slate-200"><User className="h-4 w-4 mr-1" /><span className="hidden sm:inline">Acceder / Registrarse</span></Button>}</div></div></header>;
}
